# FutManager Data

Pacotes versionados do banco SQLite e dos assets necessários para a preparação inicial do aplicativo híbrido FutManager.

O aplicativo baixa um release, valida o SHA-256 e, depois da instalação local, funciona offline.
